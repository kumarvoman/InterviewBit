#pragma once

#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

//print all the subsets of a array recursively
void subset(vector<int> A)
{

}


void RecursionRnr()
{
	vector<int> A;
	int arr[] = {1,2,3};
	for (auto x : arr)
		A.push_back(x);

	subset(A);
}
